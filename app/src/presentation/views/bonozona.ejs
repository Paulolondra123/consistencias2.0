<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <meta content="" name="keywords" />
    <meta content="" name="description" />
    <%- include('partials/links/links') %>
</head>

<body>

    <!-- Content Padre Start -->
    <div class="container-fluid position-relative d-flex p-0 padre">
        <%- include('partials/navegacion/sidebar') %>
            <!-- Content Start -->
            <div class="content position-relative">
                <%- include('partials/navegacion/navbar') %>

                    <!-- Chart Start -->
        <div class="container-fluid pt-4 px-4" id="chart">
            <div class="row g-4 mb-4">
              <div class="bg-secondary rounded p-4 col-12">
                <!-- Top div with title and button -->
                <div class="d-flex align-items-center mb-4">
                  <h6 class="mb-0">    </h6>
                </div>
                <!-- Responsive Horizontal Form -->
                <form id="formbuscar">
                  <div class="row mb-3">
                    <div class="col-md-3 mb-3">
                      <label for="inputNombre" class="form-label">Distrito:</label>
                      <select class="form-select form-select-sm mb-3" id="distrito" required>
                      </select>
                    </div>
                    <div class="col-md-2 mb-3">
                      <label for="inputGestion" class="form-label">Gestion:</label>
                      <input type="number" class="form-control" id="gestion">
                    </div>
                    <div class="col-md-2 mb-3">
                      <label for="inputApellido" class="form-label">Mes:</label>
                      <select class="form-select" id="mes">
                        <option value="1">Enero</option>
                        <option value="2">Febrero</option>
                        <option value="3">Marzo</option>
                        <option value="4">Abril</option>
                        <option value="5">Mayo</option>
                        <option value="6">Juni</option>
                        <option value="7">Julio</option>
                        <option value="8">Agosto</option>
                        <option value="9">Septiembre</option>
                        <option value="10">Octubre</option>
                        <option value="11">Noviembre</option>
                        <option value="12">Diciembre</option>
                      </select>
                    </div>
                    <div class="col-md-3 mb-3 d-flex align-items-end">
                      <button type="submit" class="btn btn-primary col-md-6">Buscar</button>
                    </div>
                  </div>
                </form>
                <div class="row mb-3">
                  <div class="row mb-3"></div>
                    <div class="col-md-6 mb-3 align-items-end">
                      <label for="nombres" class="form-label">BUASCAR MAESTROS CON BONO ZONA</label>
                      <input type="text" class="form-control col-md-5" placeholder="CODIGO SIE" id="coddis" required>
                    </div>
                    <form id="buscarmaestros" class="col-md-6 mb-3 align-items-end d-flex">
                      <button type="submit" class="btn btn-primary col-md-3">Buscar</button>
                    </form>
                  </div>
                  <form class="col-md-12 mb-3 align-items-end d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary col-md-2" id="imprimir" style="display: none;">Descargar</button>
                  </form>
                </div>
              </div>
            </div>
          <div class="container-fluid pt-4 px-4" id="chart">
            <div class="row g-4">
              <div class="bg-secondary text-center rounded p-4">
                
                <!-- <form >
                  <div class="row mb-3 gap-3">
                  </div>
                  <div class="mb-3 gap-3 d-flex ">
                    <div class="col-md-5 mb-3 d-flex align-items-center gap-5">
                      <label for="inputApellido" class="form-label">SIE:</label>
                      <input type="text" class="form-control" id="sie" placeholder="Ingrese codigo SIE">
                    </div>
                    <div class="col-md-5 mb-3 d-flex ">
                      <button type="submit" class="btn btn-primary col-md-3">Buscar</button>
                    </div>
                  </div>
                  <div class="row mb-3 gap-3">
                    <div class="col-md-8 mb-3 d-flex gap-3">
                      <label for="inputApellido" class="form-label">U.E. con bono zona:</label>
                      <input type="text" class="form-control" id="UE" placeholder="">
                    </div>
                  </div>
                </form> -->
                <div class="table-responsive">
                  <table id="myTable" class="display table align-middle table-bordered table-hover mb-0">
                    <thead>
                      <tr class="text-white">
                        <th>DISTRITO</th>
                        <th>UNIDADA EDUCATIVA</th>
                      </tr>
                    </thead>
                    <tbody id="medida">
                      
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
          <!-- Chart End -->
                      
  
                    <%- include('partials/footer/footer') %>
            </div>
            <!-- Content End -->
            <!-- Back to Top -->
            <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>
            <!-- Back to Top -->
    </div>
    <!-- Content Padre End -->
    <%- include('partials/scripts/scripts') %>
    <script>
      // JavaScript para mostrar/ocultar el formulario
      document.addEventListener("DOMContentLoaded", function () {
        const btnAgregarUsuario = document.querySelector("#btnAgregarUsuario");
        const formAgregarUsuario = document.querySelector("#myForm");
        const contenidoPadre = document.querySelector(".padre");

        btnAgregarUsuario.addEventListener("click", function (event) {
          event.stopPropagation(); // Detiene la propagación del evento
          formAgregarUsuario.classList.add("visuali"); // Muestra el formulario con animación
          contenidoPadre.classList.add("blur"); // Aplica el efecto de desenfoque al contenedor padre
        });

        formAgregarUsuario.addEventListener("submit", function (event) {
          event.stopPropagation(); // Detiene la propagación del evento
          formAgregarUsuario.classList.remove("visuali"); // Oculta el formulario después de enviarlo
          contenidoPadre.classList.remove("blur"); // Elimina el efecto de desenfoque del contenedor padre
          // Aquí puedes agregar código para manejar el envío del formulario
        });

        window.addEventListener("click", function (event) {
          if (
            !event.target.matches("#btnAgregarUsuario") &&
            !formAgregarUsuario.contains(event.target) &&
            !event.target.closest(".select2-container")
          ) {
            if (formAgregarUsuario.classList.contains("visuali")) {
              formAgregarUsuario.classList.remove("visuali"); // Quita la clase para ocultar el formulario
              contenidoPadre.classList.remove("blur"); // Elimina el efecto de desenfoque del contenedor padre
            }
          }
        });
      });
    </script>
    <script src="/js/bonozona.js"></script>
</body>

</html>